import 'package:flutter/material.dart';

class PinInput extends StatelessWidget {
  const PinInput({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('핀 입력 스크린 입니다.'),
    );
  }
}
